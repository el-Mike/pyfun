from typing import Tuple

class PygameSurface:

  # source can be any drawable object. No specifics so far.
  def blit(
    self,
    source: any,
    dest: Tuple[int, int],
    area: any,
    special_flags: int
  ) -> any:
    pass
